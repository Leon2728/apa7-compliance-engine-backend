services:
  - type: web
    name: apa7-compliance-engine
    env: python
    pythonVersion: "3.11"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
    plan: free
    autoDeploy: true
